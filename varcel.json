{
    "buildCommand" : "cd client && npm install && npm rum build",
    "outputDirectory" : "client/dist",
    "framework" : "npm",
    "rewrites" :[
        {
            "source":"/api/(.*)",
            "destination":"/api/index.js"
        }
    ]

}